Trigger the NavPlot build

## Setup

1. Create Cloudflare worker linked to the repository

2. Add NAVPLOT_WEB_HOOK variable for triggering NavPlot build (NB deploy variable
   **not** build variable.)
